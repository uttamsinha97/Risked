#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{
    printf("enter the first string\n\n");
    gets(str1);
    printf("eneter the second string]\n\n");
    gets(str2);
    myFunc(str1,str2);
    printf("")
}
